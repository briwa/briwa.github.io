<!DOCTYPE html>
<html lang="en">
<head>
    {% include "partials/meta.njk" %}{% include "styles/post/index.njk" %}
    <link rel="stylesheet" href="https://unpkg.com/prismjs@1.20.0/themes/prism-okaidia.css">
    <title>{{ title }}</title>
<body>
    <main class="content">
        <header>
            <h1>{{ title }}</h1>
            <time>{{ page.date.toDateString() }}</time>
            {% if original_url %}
            <h4>Originally posted on <a href="{{ original_url }}" target="_blank" rel="noreferrer">{{ original_url }}</a></h4>
            {% endif %}
            <hr>
        </header>
        <article class="markdown-body">
            {{ content | safe }}
        </article>
    </main>
</body>
</html>
